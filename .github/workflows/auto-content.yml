name: Auto Update Content

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/generate-content.js
      - run: git add content-manifest.json
